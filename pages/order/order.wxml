<view class="tabs">
    <block wx:for="{{ tabs }}" wx:key="type">
        <view bindtap="changeTabs" 
        data-type="{{ item.type }}" 
        class="tabs-item {{ activeType === item.type ? 'active' : '' }}">
            {{ item.text }}
        </view>
    </block>
</view>